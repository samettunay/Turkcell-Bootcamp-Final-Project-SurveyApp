@model QuestionDisplayResponse

@Model.QuestionText

@foreach (var questionOption in Model.QuestionOptions)
{
    <br />
    <input type="checkbox" id="question-option" name="AnswerTexts" value="@questionOption.Value">
    @questionOption.Value
}
